apiVersion: v1
kind: Service
metadata:
  name: nursinghome-app-service
  namespace: apps
spec:
  selector:
    app: nursinghome-app  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5002